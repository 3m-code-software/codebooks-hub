# تقرير نظام CodeBooks Hub - مكتبة البرومبتات

## نظرة عامة على المشروع

**CodeBooks Hub** هو تطبيق ويب لإدارة مكتبة البرومبتات (الأوامر النصية) المستخدمة في توليد محتوى تعليمي عربي для منصة فيسبوك. يهدف النظام إلى حفظ اتساق الهوية البصرية للبراند وتسهيل عملية إنتاج المنشورات التعليمية.

---

## أهداف المشروع

1. **إدارة البرومبتات**: إنشاء وتعديل وحذف البرومبتات المستخدمة في توليد الصور
2. **تنظيم المحتوى**: تقسيم البرومبتات إلى فئات (محاسبة، مخازن، ERP، برمجة)
3. **تسريع الإنتاج**: توفير برومبتات جاهزة مع متغيرات قابلة للتخصيص
4. **الحفاظ على الاتساق**: ضمان بقاء الهوية البصرية ثابتة عبر جميع المنشورات

---

## الهيكل العام للنظام

```
┌─────────────────────────────────────────────────────────┐
│                    المتصفح (العميل)                      │
│  ┌───────────────────────────────────────────────────┐  │
│  │              واجهة المستخدم (React)               │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ │  │
│  │  │ الداشبورد│ │ البرومبتات│ │ المحرر │ │ النسخ  │ │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
│                           │                             │
│                           ▼                             │
│  ┌───────────────────────────────────────────────────┐  │
│  │            طبقة API (TypeScript)                  │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ │  │
│  │  │  جلب    │ │  إنشاء  │ │  تعديل  │ │  حذف   │ │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                 خادم Momen (BaaS)                       │
│  ┌───────────────────────────────────────────────────┐  │
│  │           GraphQL API                             │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ │  │
│  │  │ queries │ │mutations│ │  types  │ │  schema │ │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │           قاعدة البيانات (PostgreSQL)              │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │              جدول prompt                    │  │  │
│  │  │  id | title | category | badge | content    │  │  │
│  │  │  variables | created_at | updated_at        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## التقنيات المستخدمة

### الفرونت اند (الواجهة الأمامية)

| التقنية | الاستخدام | السبب |
|---------|-----------|-------|
| **Next.js 16** | إطار العمل الرئيسي | دعم SSR و الأداء العالي |
| **React 19** | بناء الواجهات | مكتبة UI الم标准 |
| **TypeScript** | اللغة البرمجية | نوع بيانات صارم وأمان |
| **Tailwind CSS 4** | التصميم | تنسيق سريع ومتجاوب |

### الباك اند (الخادم)

| التقنية | الاستخدام | السبب |
|---------|-----------|-------|
| **Momen BaaS** | منصة الخادم | لا حاجة لبرمجة خادم منفصل |
| **GraphQL** | لغة الاستعلامات | مرونة في جلب البيانات |
| **PostgreSQL** | قاعدة البيانات | موثوقة وقوية |

### الاستضافة

| الخدمة | الاستخدام |
|--------|-----------|
| **Vercel** | استضافة الفرونت اند |
| **Momen** | استضافة الباك اند وقاعدة البيانات |
| **GitHub** | التحكم في الإصدارات |

---

## شرح الملفات والمجلدات

### هيكل المشروع

```
codebooks-hub/
├── src/
│   ├── app/                    # صفحات التطبيق
│   │   ├── globals.css         # الأنماط العامة والهوية البصرية
│   │   ├── layout.tsx          # التخطيط الرئيسي (RTL + عربي)
│   │   └── page.tsx            # الصفحة الرئيسية (Dashboard)
│   ├── components/             # مكونات الواجهة
│   │   ├── Header.tsx          # الشريط العلوي مع البحث
│   │   ├── Sidebar.tsx         # الشريط الجانبي للفئات
│   │   ├── Stats.tsx           # عرض الإحصائيات
│   │   ├── PromptCard.tsx      # بطاقة عرض البرومبت
│   │   ├── PromptEditor.tsx    # محرر البرومبتات
│   │   └── QuickCopy.tsx       # نافذة النسخ السريع
│   └── lib/                    # الملفات المساعدة
│       ├── types.ts            # تعريف الأنواع والفئات
│       ├── api.ts              # التكامل مع Momen API
│       └── prompts-data.ts     # البرومبتات الافتراضية
├── .env.local                  # متغيرات البيئة (مخفية)
└── package.json                # حزم المشروع
```

---

## شرح كل ملف بالتفصيل

### 1. ملف types.ts - تعريف الأنواع

**الغرض**: تعريف هيكل البيانات المستخدم في النظام

```typescript
// تعريف الفئات الأربع
type Category = "accounting" | "inventory" | "erp" | "programming";

// تعريف البرومبت
interface Prompt {
  id: string;           // المعرف الفريد
  title: string;        // العنوان
  category: Category;   // الفئة
  badge: string;        // البادج (مثال: سؤال محاسبي مهم)
  content: string;      // محتوى البرومبت الكامل
  variables: PromptVariable[];  // المتغيرات القابلة للتخصيص
  createdAt: Date;      // تاريخ الإنشاء
  updatedAt: Date;      // تاريخ آخر تعديل
}

// تعريف المتغير
interface PromptVariable {
  name: string;         // اسم المتغير
  placeholder: string;  // القيمة الافتراضية
  type: "text" | "textarea" | "select";  // نوع الإدخال
  options?: string[];   // خيارات (للقوائم المنسدلة)
}
```

**لماذا هذا مهم؟**: يضمن أن جميع البيانات متوافقة ويمنع الأخطاء البرمجية

---

### 2. ملف api.ts - التكامل مع الباك اند

**الغرض**: التعامل مع Momen GraphQL API

**كيف يعمل**:

1. **الاتصال بالخادم**:
   - الرابط: `https://villa.momen.app/zero/{projectExId}/api/graphql-v2`
   - المصادقة: `Bearer Token` في الـ Header

2. **العمليات الأساسية**:

| الدالة | الغرض | GraphQL |
|--------|-------|---------|
| `fetchPrompts()` | جلب جميع البرومبتات | `query { prompt { ... } }` |
| `createPrompt()` | إنشاء برومبت جديد | `mutation { insert_prompt_one(...) }` |
| `updatePrompt()` | تعديل برومبت | `mutation { update_prompt_by_pk(...) }` |
| `deletePrompt()` | حذف برومبت | `mutation { delete_prompt_by_pk(...) }` |

3. **معالجة الأخطاء**:
   - إذا فشل الاتصال بالباك اند، يستخدم البرومبتات الافتراضية
   - يتم تسجيل الأخطاء في Console

---

### 3. ملف globals.css - الأنماط العامة

**الغرض**: تعريف الهوية البصرية للموقع

**الألوان الأساسية**:

| اللون | الكود | الاستخدام |
|-------|-------|-----------|
| أسود داكن | `#1a1a1a` | الخلفية الرئيسية |
| ذهبي | `#D4A843` | اللون الرئيسي والتمييز |
| أبيض | `#ffffff` | النصوص |
| أزرق داكن | `#1e3a5f` | دعم ثانوي |
| أخضر داكن | `#1a6b6b` | دعم ثانوي |

**المؤثرات**:
- `glass`: تأثير الزجاج (خلفية شفافة + ضبابية)
- `gradient-text`: نص متدرج بالذهبي
- `card-hover`: تأثير عند مرور الماوس
- `glow`: توهج خفيف

---

### 4. ملف layout.tsx - التخطيط الرئيسي

**الغرض**: التخطيط العام لجميع الصفحات

**الميزات**:
- دعم RTL (من اليمين لليسار) للعربية
- تحميل الخطوط (Geist)
- إعداد اللغة العربية

---

### 5. ملف page.tsx - الصفحة الرئيسية

**الغرض**: Dashboard الرئيسي للتطبيق

**كيف يعمل**:

```
┌──────────────────────────────────────────────────┐
│  Header (بحث + زر إضافة جديد)                    │
├──────────────────────────────────────────────────┤
│  Stats (إحصائيات: إجمالي، فئات، متغيرات)         │
├──────────────────────────────────────────────────┤
│  PromptCards (شبكة البرومبتات)                    │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐            │
│  │ برومبت 1│ │ برومبت 2│ │ برومبت 3│            │
│  └─────────┘ └─────────┘ └─────────┘            │
└──────────────────────────────────────────────────┘
```

**الحالة (State)**:

| المتغير | الغرض |
|---------|-------|
| `prompts` | جميع البرومبتات من الباك اند |
| `filteredPrompts` | البرومبتات بعد الفلترة |
| `activeCategory` | الفئة المحددة حالياً |
| `searchQuery` | نص البحث |
| `selectedPrompt` | البرومبت المحدد للتعديل |

**التدفق**:

1. **التحميل الأولي**: `useEffect` يستدعي `fetchPrompts()`
2. **الفلترة**: كل ما يتغير `activeCategory` أو `searchQuery`
3. **الحفظ**: `handleSave()` يستدعي `createPrompt()` أو `updatePrompt()`
4. **الحذف**: `handleDelete()` يستدعي `deletePrompt()`

---

### 6. مكون Sidebar.tsx - الشريط الجانبي

**الغرض**: التنقل بين الفئات

**المحتويات**:
- شعار البراند (CodeBooks)
- أزرار الفئات (الكل، محاسبة، مخازن، ERP، برمجة)
- إمكانية الطي (Collapse)

**كيف يعمل**:
- يستقبل `activeCategory` و `onCategoryChange` كـ props
- عند النقر على فئة، يستدعي `onCategoryChange` لتحديث الحالة في الصفحة الرئيسية

---

### 7. مكون Header.tsx - الشريط العلوي

**الغرض**: البحث وإضافة البرومبتات الجديدة

**المكونات**:
- حقل البحث (实时 filtering)
- زر "برومبت جديد"

---

### 8. مكون Stats.tsx - الإحصائيات

**الغرض**: عرض ملخص سريع للمحتوى

**تعرض**:
- إجمالي عدد البرومبتات
- عدد البرومبتات التي بها متغيرات
- عدد البرومبتات في كل فئة

---

### 9. مكون PromptCard.tsx - بطاقة البرومبت

**الغرض**: عرض البرومبت في شكل بطاقة

**المحتويات**:
- فئة البرومبت (محاسبة، مخازن، etc.)
- البادج
- العنوان
- مقتطف من المحتوى
- أزرار (نسخ، تحرير، حذف)

**التفاعل**:
- النقر على البطاقة → فتح QuickCopy
- زر "نسخ" → نسخ المحتوى للحافظة
- زر "تحرير" → فتح PromptEditor
- زر "حذف" → تأكيد ثم الحذف

---

### 10. مكون PromptEditor.tsx - محرر البرومبتات

**الغرض**: تعديل البرومبتات بالتفصيل

**التصميم**:

```
┌──────────────────────────────────────────────────────┐
│  عنوان: تحرير البرومبت                        [✕]   │
├──────────────────────────────────────────────────────┤
│  ┌─────────────────────┐  ┌─────────────────────┐   │
│  │   حقول الإدخال      │  │    المعاينة المباشرة  │   │
│  │                     │  │                     │   │
│  │  [العنوان]          │  │  النص بعد استبدال   │   │
│  │  [الفئة ▼]         │  │  المتغيرات          │   │
│  │  [البادج]           │  │                     │   │
│  │  [المحتوى]          │  │                     │   │
│  │  [المتغيرات]        │  │                     │   │
│  └─────────────────────┘  └─────────────────────┘   │
├──────────────────────────────────────────────────────┤
│              [إلغاء]              [حفظ التغييرات]     │
└──────────────────────────────────────────────────────┘
```

**الميزات**:
1. **معاينة مباشرة**: كل ما يتغير المحتوى أو المتغيرات، تظهر النتيجة فوراً
2. **إدارة المتغيرات**: يمكن تغيير قيم المتغيرات (`{{variable}}`)
3. **نسخ المعاينة**: زر لنسخ النتيجة النهائية

---

### 11. مكون QuickCopy.tsx - النسخ السريع

**الغرض**: عرض البرومبت بالكامل ونسخه بسرعة

**المحتويات**:
- محتوى البرومبت الكامل
- زر "نسخ الكل"

---

## دورة حياة البيانات

### 1. جلب البيانات (Read)

```
المستخدم يفتح التطبيق
        │
        ▼
page.tsx يستدعي fetchPrompts()
        │
        ▼
api.ts يرسل GraphQL Query
        │
        ▼
Momen API يستعلم من PostgreSQL
        │
        ▼
البيانات تظهر في الواجهة
```

### 2. إنشاء بيانات جديدة (Create)

```
المستخدم يضغط "برومبت جديد"
        │
        ▼
يظهر PromptEditor
        │
        ▼
المستخدم يملأ الحقول ويضغط "حفظ"
        │
        ▼
api.ts يرسل GraphQL Mutation
        │
        ▼
Momen API يحفظ في PostgreSQL
        │
        ▼
البيانات الجديدة تظهر في الواجهة
```

### 3. تعديل البيانات (Update)

```
المستخدم يضغط "تحرير" على برومبت
        │
        ▼
يظهر PromptEditor مع البيانات الحالية
        │
        ▼
المستخدم يعدل ويضغط "حفظ"
        │
        ▼
api.ts يرسل GraphQL Mutation Update
        │
        ▼
Momen API يحدث في PostgreSQL
        │
        ▼
البيانات المحدثة تظهر في الواجهة
```

### 4. حذف البيانات (Delete)

```
المستخدم يضغط "حذف" على برومبت
        │
        ▼
يظهر تأكيد الحذف
        │
        ▼
المستخدم يؤكد
        │
        ▼
api.ts يرسل GraphQL Mutation Delete
        │
        ▼
Momen API يحذف من PostgreSQL
        │
        ▼
البرومبت يختفي من الواجهة
```

---

## نظام المتغيرات

### كيف يعمل

البرومبتات قد تحتوي على متغيرات بصيغة `{{variable}}`. عند التعديل، يمكن تغيير هذه المتغيرات.

**مثال**:

```
البرومبت الأصلي:
"Generate a post about {{topic}} with {{style}} style"

المتغيرات:
- topic: "Accounting" (القيمة الافتراضية: "المحاسبة")
- style: "Premium" (القيمة الافتراضية: "فاخر")

النتيجة بعد الاستبدال:
"Generate a post about Accounting with Premium style"
```

### أنواع المتغيرات

| النوع | الاستخدام |
|-------|-----------|
| `text` | حقل نصي قصير |
| `textarea` | حقل نصي طويل |
| `select` | قائمة منسدلة |

---

## نظام الفئات

| الفئة | الرمز | اللون | الأيقونة |
|-------|-------|-------|----------|
| محاسبة | `accounting` | ذهبي `#D4A843` | 📊 |
| مخازن | `inventory` | أخضر داكن `#1a6b6b` | 📦 |
| ERP | `erp` | أزرق داكن `#1e3a5f` | 🔄 |
| برمجة | `programming` | بنفسجي `#8b5cf6` | 💻 |

---

## كيفية التشغيل المحلي

### المتطلبات
- Node.js 18+
- npm

### خطوات التشغيل

```bash
# 1. نسخ المشروع
git clone https://github.com/3m-code-software/codebooks-hub.git

# 2. الدخول للمجلد
cd codebooks-hub

# 3. تثبيت الحزم
npm install

# 4. تشغيل الخادم المحلي
npm run dev

# 5. فتح المتصفح
# http://localhost:3000
```

---

## كيفية النشر على Vercel

### الخطوات

1. **رفع المشروع على GitHub** (تم)
2. **فتح Vercel**: https://vercel.com/new
3. **اختيار المشروع**: Import Git Repository
4. **اختيار المستودع**: `codebooks-hub`
5. **إضافة متغيرات البيئة**:

| المتغير | القيمة |
|---------|--------|
| `NEXT_PUBLIC_MOMEN_API_URL` | رابط GraphQL API |
| `NEXT_PUBLIC_MOMEN_ADMIN_TOKEN` | Admin Token |

6. **النشر**: Deploy

---

## كيف تضيف ميزات جديدة

### مثال 1: إضافة فئة جديدة

1. افتح `src/lib/types.ts`
2. أضف الفئة في `CATEGORIES`

```typescript
{
  id: "database",
  name: "قواعد بيانات",
  nameEn: "Database",
  icon: "🗄️",
  color: "#e74c3c",
}
```

3. أضف البرومبتات الافتراضية في `src/lib/prompts-data.ts`

### مثال 2: إضافة حقل جديد للبرومبت

1. أضف الحقل في `types.ts`

```typescript
interface Prompt {
  // ... الحقول الموجودة
  tags: string[];  // حقل جديد
}
```

2. حدّث `api.ts` ليشمل الحقل الجديد في GraphQL
3. حدّث `PromptCard.tsx` لعرض الحقل الجديد

### مثال 3: إضافة صفحة جديدة

1. أنشئ ملف جديد في `src/app/`

```
src/app/prompts/[id]/page.tsx
```

2. استورد المكونات اللازمة
3. أضف الرابط في `Sidebar.tsx`

---

## مشاكل شائعة وحلولها

### المشكلة 1: التطبيق لا يعرض البرومبتات

**السبب**: خطأ في الاتصال بالباك اند

**الحل**:
1. تحقق من `NEXT_PUBLIC_MOMEN_API_URL`
2. تحقق من `NEXT_PUBLIC_MOMEN_ADMIN_TOKEN`
3. افحص Console في المتصفح للأخطاء

### المشكلة 2: لا يمكن حفظ البرومبتات

**السبب**: خطأ في GraphQL Mutation

**الحل**:
1. تحقق من اسم الجدول (`prompt`)
2. تحقق من أسماء الحقول
3. افحص Runtime Logs في Momen

### المشكلة 3: التصميم لا يظهر بشكل صحيح

**السبب**: مشكلة في CSS أو RTL

**الحل**:
1. تحقق من `dir="rtl"` في `layout.tsx`
2. تحقق من `lang="ar"` في `layout.tsx`
3. امسح Cache المتصفح

---

## خطة التطوير المستقبلية

### المرحلة الأولى (قريبة)

- [ ] إضافة نظام بحث متقدم
- [ ] إضافة فلترة حسب التاريخ
- [ ] إضافة تصدير البرومبتات (JSON/CSV)
- [ ] إضافة تاريخ التعديلات (Version History)

### المرحلة الثانية (متوسطة)

- [ ] إضافة نظام مستخدمين (تسجيل دخول)
- [ ] إضافة صلاحيات (مدير، محرر، مشاهد)
- [ ] إضافة تعليقات على البرومبتات
- [ ] إضافة تصنيفات فرعية

### المرحلة الثالثة (بعيدة)

- [ ] ربط مع Google AI Studio API لتوليد الصور تلقائياً
- [ ] إضافة معاينة الصور المولدة
- [ ] إضافة جدولة المنشورات
- [ ] إضافة تحليلات الاستخدام

---

## معلومات الاتصال

- **المطور**: 3m-code-software
- **GitHub**: https://github.com/3m-code-software/codebooks-hub
- **Momen Dashboard**: https://editor.momen.app

---

## مصطلحات فنية

| المصطلح | المعنى |
|---------|--------|
| **BaaS** | Backend as a Service - خادم كخدمة |
| **GraphQL** | لغة استعلام للمعاملات |
| **RTL** | Right to Left - من اليمين لليسار |
| **SSR** | Server Side Rendering - عرض من الخادم |
| **JSONB** | JSON Binary - نوع بيانات JSON في PostgreSQL |
| **CRUD** | Create, Read, Update, Delete - العمليات الأساسية |
| **Component** | مكون - وحدة UI قابلة لإعادة الاستخدام |
| **State** | حالة البيانات في التطبيق |
| **Props** | خصائص تمرر للمكونات |

---

*آخر تحديث: يوليو 2024*
